#ifndef __PRINT_VECTORS
#define __PRINT_VECTORS

/**
 * @brief print a integer's vector in terminal, with elements separated by spaces
 * 
 * @param vector vector of elements
 * @param n number of elements
 * 
 * @pre vector!=NULL
 */
void printV(int *vector, int n);

#endif